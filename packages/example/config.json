{
  "transforms": [
    {
      "mode": "sql",
      "include": "**/*.sql",
      "emitTemplate": "{{dir}}/{{name}}_queries.rs"
    }
  ],
  "srcDir": "./src/",
  "db": {
    "dbName": "postgres",
    "user": "postgres",
    "password": "postgres",
    "host": "localhost",
    "port": 5432,
    "ssl": false
  }
}